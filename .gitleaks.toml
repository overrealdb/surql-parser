[extend]
useDefault = true

[allowlist]
description = "SurrealQL doc examples use example JWT key pairs from SurrealDB docs, not real secrets"
paths = [
    '''tests/fixtures/doc-examples/.*\.surql''',
]
