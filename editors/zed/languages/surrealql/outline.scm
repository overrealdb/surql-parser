; Code outline — shows DEFINE statements in the sidebar breadcrumb

(define_table_statement
  (keyword_table)
  (identifier) @name) @item

(define_field_statement
  (keyword_field)
  (identifier) @name) @item

(define_index_statement
  (keyword_index)
  (identifier) @name) @item

(define_function_statement
  (keyword_function)
  (identifier) @name) @item

(define_event_statement
  (keyword_event)
  (identifier) @name) @item

(define_analyzer_statement
  (keyword_analyzer)
  (identifier) @name) @item

(define_namespace_statement
  (keyword_namespace)
  (identifier) @name) @item

(define_database_statement
  (keyword_database)
  (identifier) @name) @item

(define_scope_statement
  (keyword_scope)
  (identifier) @name) @item

(define_param_statement
  (keyword_param)
  (param) @name) @item
